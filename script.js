/*global $*/

// ** empty() - empties the inner HTML of an example
$(".button").empty();
$("#points-of-sale").empty();

// ** remove() - removes the element completely
$(".button").empty();
$("#contact img").empty();
